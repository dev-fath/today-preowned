import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.today.preowned',
  appName: 'today-preowned',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
